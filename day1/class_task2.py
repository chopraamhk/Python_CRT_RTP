import random
import sys

def main():
      dictionary = dict(name = "John", age = 36, country = "Norway")
      print(f"{dictionary}!")

def square_dictionary(a_dict):
      result_dict = dict()  #can use {} also,
      for key_value in a_dict.items():
            result_dict[key] = value * value
          return result_dict

      for name

      #map(lambda : ))